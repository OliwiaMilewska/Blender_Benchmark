blender_path: "/home/oliwialinux/Documents/blender-5.1.2-linux-x64/blender"

scene: "data/references/lightsaber.blend"

engine: "CYCLES"

cycles_settings:
  device: "GPU"
  samples: 512

reference: "data/references/ref/lightsaber_Cycles_GPU_512.png"

repeat: 10

wait: 600
